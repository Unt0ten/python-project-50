name: Python-CI

on:
  - push
  - pull_request

jobs:
    steps:
        # make depends on poetry
      - name: Install dependencies
        run: |
          pip install poetry
          poetry install
          make lint-install
      - name: Run linter and pytest
        run: |
          make lint
