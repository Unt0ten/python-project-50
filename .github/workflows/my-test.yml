name: Python CI

on:
  - push
  - pull_request

jobs:
  my_testing:
    runs-on: ubuntu-latest
    steps:
        # make depends on poetry
      - name: Install dependencies
        run: |
          pip install poetry
          poetry install
          make build
      - name: Run linter and pytest
        run: |
          make lint
